我需要为之前的文档添加更多规范和建议，并在文档最后新增"添加原因"部分。我将基于提供的信息和常见的 Vue3+TypeScript 开发规范来补充。

首先，我需要检查之前的文档，看看还有哪些规范和建议可以添加：

1. **TypeScript 类型定义规范**：在之前的文档中已经提到了类型定义，但可以更详细地说明如何组织和使用类型。

2. **API 设计规范**：可以补充 API 请求和响应的类型定义规范。

3. **状态管理规范**：可以补充 Pinia 的使用规范。

4. **错误处理规范**：可以补充错误处理的最佳实践。

5. **国际化支持**：可以补充国际化相关的规范。

6. **性能优化建议**：可以补充更多性能优化的建议。

7. **安全规范**：可以补充安全相关的规范。

8. **文档编写规范**：可以补充如何编写高质量的文档。

9. **团队协作规范**：可以补充团队协作相关的规范。

现在，我将为文档添加这些内容，并在最后新增"添加原因"部分。

让我先思考一下这些补充内容：

1. **TypeScript 类型定义规范**：

   - 推荐使用`interface`定义对象结构，使用`type`定义联合类型
   - 统一使用`type`定义 API 响应类型
   - 为每个模块创建独立的类型定义文件

2. **API 设计规范**：

   - 统一 API 响应格式（成功/失败结构）
   - 使用`Axios`或`fetch`封装请求
   - 为每个 API 端点创建独立的类型

3. **状态管理规范**（Pinia）：

   - 每个模块有独立的 store
   - 使用`defineStore`定义 store
   - 使用`state`、`getters`、`actions`的结构

4. **错误处理规范**：

   - 统一错误处理中间件
   - 为不同错误类型定义错误码
   - 提供友好的错误提示

5. **国际化支持**：

   - 使用`vue-i18n`进行国际化
   - 按语言包组织翻译文件
   - 统一使用`$t`方法获取翻译

6. **性能优化建议**：

   - 使用`v-for`的`key`属性
   - 避免在模板中使用复杂表达式
   - 使用`v-memo`优化列表渲染

7. **安全规范**：

   - 防止 XSS 攻击（使用`v-html`时需注意）
   - 对用户输入进行过滤
   - 使用 CSP（内容安全策略）

8. **文档编写规范**：

   - 使用 JSDoc 规范注释
   - 为每个组件和函数编写文档
   - 保持文档与代码同步

9. **团队协作规范**：
   - 使用 Git 分支规范
   - 提交信息规范
   - 代码审查流程

现在，我将把这些内容整合到文档中，并在最后添加"添加原因"部分。

我将主要在"三、代码规范"部分添加类型定义规范和 API 设计规范，在"四、目录结构最佳实践"部分添加状态管理规范，然后在文档最后添加"添加原因"部分。

让我开始编写这些补充内容。

# Vue 3 + TypeScript 项目开发规范与架构指南

## 一、项目概述与技术栈

### 核心技术栈

- **框架**: Vue 3 + Composition API
- **语言**: TypeScript
- **状态管理**: Pinia (推荐) 或 Vuex
- **构建工具**: Vite
- **路由**: Vue Router 4
- **样式**: 可选 Tailwind CSS / SCSS
- **测试**: Vitest + Vue Test Utils

### 架构特点

- 🏗️ **模块化设计** - 基于功能的功能模块架构
- 🎯 **类型安全** - 完整的 TypeScript 支持
- 🔧 **可维护性** - 清晰的代码组织和分层
- 🧪 **可测试性** - 模块化的测试友好设计
- 🚀 **开发体验** - 优秀的开发工具和热重载
- 📦 **代码复用** - 高度的组件和逻辑复用

## 二、项目目录结构

### 完整目录结构

```plaintext
project-root/
├── public/ # 静态资源目录
│   ├── favicon.ico
│   ├── logo.png
│   └── manifest.json
├── src/ # 源代码目录
│   ├── assets/ # 静态资源（经过构建处理）
│   │   ├── images/ # 图片资源
│   │   ├── styles/ # 全局样式
│   │   └── fonts/ # 字体文件
│   ├── components/ # 全局共享组件
│   ├── composables/ # 全局共享组合式函数
│   ├── config/ # 应用配置文件
│   ├── features/ # 功能模块（核心业务代码）
│   │   ├── auth/ # 认证功能模块
│   │   ├── user/ # 用户功能模块
│   │   ├── product/ # 产品功能模块
│   │   ├── order/ # 订单功能模块
│   │   └── dashboard/ # 仪表板功能模块
│   ├── layouts/ # 页面布局组件
│   ├── lib/ # 第三方库配置
│   ├── pages/ # 页面组件
│   ├── plugins/ # Vue 插件
│   ├── router/ # 路由配置
│   ├── services/ # 应用服务层
│   ├── stores/ # 全局状态管理
│   ├── test/ # 测试相关文件
│   ├── types/ # 全局类型定义
│   ├── utils/ # 工具函数
│   ├── App.vue # 根组件
│   └── main.ts # 应用入口
├── tests/ # 测试目录
├── docs/ # 项目文档
├── .env # 环境变量
├── .env.development # 开发环境变量
├── .env.production # 生产环境变量
├── package.json
├── tsconfig.json # TypeScript 配置
├── vite.config.ts # Vite 构建配置
├── vitest.config.ts # 测试配置
└── README.md
```

## 三、代码规范

### 3.1 编码风格与语法规范

#### 3.1.1 箭头函数

- **推荐使用箭头函数**定义方法，避免 `this` 指向混乱
- **禁止混用** `function` 与箭头函数，保持统一性

```typescript
// ✅ 推荐
const fetchData = () => {
  /* ... */
};

// ❌ 不推荐
function fetchData() {}
const fetchData2 = () => {};
```

#### 3.1.2 变量声明

- 使用 `const` 声明常量或不可变值，`let` 声明可变变量
- **禁止使用 `var`**，避免变量提升问题

#### 3.1.3 异步与数据请求

- 所有异步操作必须使用 `try...catch` 或 `.catch()` 捕获异常
- **禁止回调地狱**，优先使用 `async/await`
- 并发请求使用 `Promise.all`；若需全部完成（无论成功失败），使用 `Promise.allSettled`
- 接口方法命名建议以 `Api` 结尾，如 `getUserListApi`

#### 3.1.4 响应式变量选择

- 大型嵌套对象/数组，若只需顶层响应，使用 `shallowRef` / `shallowReactive`
- 避免对大数据无差别使用 `ref` / `reactive`，减少性能开销

#### 3.1.5 单一职责原则

- 组件/函数应只负责单一功能，便于测试、复用和维护

### 3.2 组件与文件组织

#### 3.2.1 文件与组件命名

- 组件文件名采用 **大驼峰（PascalCase）**，如 `UserTable.vue`
- 所有组件（包括入口组件）**必须设置 `name` 属性**，且全局唯一

```vue
<script setup name="UserManagement"></script>
<!-- 或 -->
export default defineComponent({ name: 'UserManagement' })
```

#### 3.2.2 组合式 API 书写顺序（`<script setup>`）

按以下顺序组织代码：

1. `import` 语句
2. `defineProps` / `defineEmits`
3. 响应式变量（`ref`, `reactive` 等）
4. `computed`
5. `watch` / `watchEffect`
6. 函数定义
7. 生命周期钩子（`onMounted` 等）
8. `defineExpose`

#### 3.2.3 Hooks 封装

- 逻辑复用优先使用 **自定义 Composables（Hooks）**，而非对象式状态管理
- Hook 函数以 `use` 开头，如 `useModal`、`usePagination`
- 在 Hook 内部可组合响应式状态、方法和生命周期

#### 3.2.4 组件开发规范

- 组件命名：

  - 全局基础组件：`BaseButton.vue`, `BaseInput.vue`
  - 功能特定组件：`UserProfile.vue`, `ProductCard.vue`
  - 组合组件：`UserProfileEditor.vue`, `ProductListWithFilter.vue`

- 组件 Props 定义：

```vue
<script setup lang="ts">
interface Props {
  // 必填属性
  title: string;
  items: Array<any>;
  // 可选属性
  loading?: boolean;
  maxItems?: number;
  // 复杂属性
  config?: {
    sortable: boolean;
    filterable: boolean;
  };
}

const props = withDefaults(defineProps<Props>(), {
  loading: false,
  maxItems: 10,
  config: () => ({ sortable: true, filterable: false }),
});
</script>
```

#### 3.2.5 异步组件与路由懒加载

- 复杂或非首屏组件使用 `defineAsyncComponent` 实现懒加载：

```js
const HeavyComponent = defineAsyncComponent(() =>
  import("@/components/HeavyComponent.vue")
);
```

- 路由组件使用动态导入：

```js
{
  path: '/user',
  component: () => import('@/views/User.vue')
}
```

### 3.3 逻辑与结构优化

#### 3.3.1 逻辑分支优化

- **避免多重 `if-else if` 或深层嵌套**
- 使用 **策略模式/字典映射** 替代多条件判断：

```js
const handlers = {
  1: handleA,
  2: handleB,
};
(handlers[type] || handleDefault)();
```

- 多重条件判断使用 **"早返回"原则**：

```js
if (type !== 1) return;
if (status !== "active") return;
// 主逻辑
```

#### 3.3.2 属性挑选（Pick/Omit）

- 优雅剔除属性使用解构赋值：

```js
const { unwantedProp, ...rest } = obj; // rest 不含 unwantedProp
```

#### 3.3.3 暴露组件实例方法

- 需暴露第三方组件所有方法时，可使用 `Proxy` 代理：

```js
expose(
  new Proxy(
    {},
    {
      get(_, key) {
        return internalRef.value?.[key];
      },
    }
  )
);
```

### 3.4 代码可读性与注释规范

#### 3.4.1 文件注释（顶部）

```html
<!-- 
* @FileDescription: 用户管理页面
* @Author: LiuMingXin
* @Date: 2025-02-20
* @LastEditors: LiuMingXin
* @LastEditTime: 2025-02-20
-->
```

#### 3.4.2 方法注释（JSDoc）

```js
/**
 * 获取用户列表数据
 * @param {Object} params 查询参数
 * @param {number} params.pageNum 页码
 * @return {Promise<void>}
 */
const fetchUserList = async (params) => {
  /* ... */
};
```

#### 3.4.3 变量与行内注释

- 关键变量需注释说明用途
- 复杂业务逻辑必须添加行内注释

#### 3.4.4 代码块折叠

- 高耦合逻辑使用 `// #region` / `// #endregion` 折叠：

```js
// #region 排序逻辑
const handleSort = () => {
  /* ... */
};
// #endregion
```

#### 3.4.5 功能/修复标记

```js
// FEAT-1001: 新增导出功能(LMX-2025-02-20)
// BUGFIX-2005: 修复分页失效问题(LMX-2025-02-20)
```

### 3.5 现代 JS 运算符推荐

| 运算符 | 用途             | 示例                             |
| ------ | ---------------- | -------------------------------- |
| `?.`   | 可选链           | `obj?.user?.name`                |
| `??`   | 空值合并         | `value ?? 'default'`             |
| `??=`  | 空值赋值         | `x ??= 10`                       |
| `?=`   | 安全解构（提案） | `[err, res] ?= await fetch(...)` |

> **注**：`?=` 为实验性语法，需确认环境支持。

### 3.6 类型定义规范

#### 3.6.1 接口与类型别名区分

- **定义对象/类的结构用 `interface`**（支持扩展、实现）
- **定义联合类型、交叉类型或简单类型别名用 `type`**：

```typescript
// ✅ 正确（interface 定义对象结构）
interface User {
  id: number;
  name: string;
}

interface AdminUser extends User {
  role: "admin";
}

// ✅ 正确（type 定义联合类型）
type GoodsCategory = "electronics" | "clothes" | "food";
type GoodsWithDiscount = Goods & { discount: number };
```

#### 3.6.2 泛型约束

- 使用泛型时，需通过 `extends` 添加约束，避免无意义的类型范围：

```typescript
// ✅ 正确（泛型约束为"有 id 属性的对象"）
function getById<T extends { id: number }>(
  list: T[],
  id: number
): T | undefined {
  return list.find((item) => item.id === id);
}

// ❌ 错误（泛型无约束，无法安全访问 id 属性）
function getById<T>(list: T[], id: number): T | undefined {
  return list.find((item) => item.id === id); // 报错：T 上不存在 id
}
```

#### 3.6.3 API 类型定义

- 所有 API 响应类型统一定义在 `src/types/api.types.ts`
- 为每个 API 端点创建独立类型：

```typescript
// src/types/api.types.ts
export interface User {
  id: number;
  name: string;
  email: string;
  role: "admin" | "user" | "guest";
}

export interface UserResponse {
  data: User;
  message: string;
  code: number;
}
```

### 3.7 错误处理规范

#### 3.7.1 统一错误处理

- 在 `src/services/api/errorHandler.ts` 中实现统一错误处理
- 定义错误类型和错误码：

```typescript
// src/services/api/errorHandler.ts
export enum ErrorCode {
  NETWORK_ERROR = "NETWORK_ERROR",
  AUTH_ERROR = "AUTH_ERROR",
  VALIDATION_ERROR = "VALIDATION_ERROR",
  UNKNOWN_ERROR = "UNKNOWN_ERROR",
}

export interface ApiError {
  code: ErrorCode;
  message: string;
  details?: any;
}

export const handleApiError = (error: any): ApiError => {
  // 根据错误类型返回统一错误结构
  if (error.response) {
    // 服务器响应
    return {
      code:
        error.response.status === 401
          ? ErrorCode.AUTH_ERROR
          : ErrorCode.UNKNOWN_ERROR,
      message: error.response.data.message || "请求失败",
      details: error.response.data,
    };
  } else if (error.request) {
    // 请求发送但没有响应
    return {
      code: ErrorCode.NETWORK_ERROR,
      message: "网络连接失败，请检查您的网络",
    };
  } else {
    // 其他错误
    return {
      code: ErrorCode.UNKNOWN_ERROR,
      message: error.message,
    };
  }
};
```

#### 3.7.2 错误展示

- 在 `src/components/ui/ErrorMessage.vue` 中统一展示错误
- 使用 `v-if` 控制错误显示：

```vue
<template>
  <div v-if="error" class="error-message">
    {{ error.message }}
    <button v-if="error.details" @click="showDetails = !showDetails">
      {{ showDetails ? "隐藏详情" : "显示详情" }}
    </button>
    <div v-if="showDetails" class="error-details">
      {{ JSON.stringify(error.details, null, 2) }}
    </div>
  </div>
</template>

<script setup lang="ts">
import { ref } from "vue";

interface Props {
  error: { message: string; details?: any };
}

const props = defineProps<Props>();
const showDetails = ref(false);
</script>
```

### 3.8 国际化支持规范

#### 3.8.1 统一国际化配置

- 在 `src/plugins/i18n.ts` 中初始化国际化插件

```typescript
// src/plugins/i18n.ts
import { createI18n } from "vue-i18n";
import zhCN from "./locales/zh-CN.json";
import enUS from "./locales/en-US.json";

const i18n = createI18n({
  legacy: false,
  locale: "zh-CN",
  fallbackLocale: "en-US",
  messages: {
    "zh-CN": zhCN,
    "en-US": enUS,
  },
});

export default i18n;
```

#### 3.8.2 语言包组织

- 按语言包组织翻译文件：

```plaintext
src/
└── plugins/
    └── i18n/
        ├── locales/
        │   ├── zh-CN.json
        │   └── en-US.json
        └── index.ts
```

#### 3.8.3 使用方式

- 在组件中使用 `$t` 方法：

```vue
<template>
  <div>
    <h1>{{ $t("welcome.title") }}</h1>
    <p>{{ $t("welcome.message", { name: "Vue" }) }}</p>
  </div>
</template>
```

### 3.9 性能优化建议

#### 3.9.1 组件优化

- 使用 `v-memo` 优化列表渲染：

```vue
<template>
  <div v-for="item in items" :key="item.id" v-memo="item.id">
    <!-- 组件内容 -->
  </div>
</template>
```

- 避免在模板中使用复杂表达式，将计算逻辑移到 `computed` 中：

```vue
<!-- ❌ 避免 -->
<div>{{ item.price * (1 - item.discount) }}</div>

<!-- ✅ 推荐 -->
<template>
  <div>{{ discountedPrice }}</div>
</template>

<script setup lang="ts">
import { computed } from "vue";

const discountedPrice = computed(() => {
  return props.item.price * (1 - props.item.discount);
});
</script>
```

#### 3.9.2 路由懒加载

- 路由配置中使用动态导入：

```js
{
  path: '/dashboard',
  component: () => import('@/views/Dashboard.vue'),
  meta: { title: 'Dashboard' }
}
```

### 3.10 安全规范

#### 3.10.1 防止 XSS 攻击

- 仅在需要时使用 `v-html`，并确保内容已安全处理：

```vue
<template>
  <div v-html="safeHtmlContent"></div>
</template>

<script setup lang="ts">
import { computed } from "vue";

const safeHtmlContent = computed(() => {
  // 使用安全库处理 HTML
  return sanitizeHtml(props.content);
});
</script>
```

#### 3.10.2 输入验证

- 对用户输入进行验证，避免恶意输入：

```vue
<script setup lang="ts">
import { ref } from "vue";

const email = ref("");
const emailError = ref("");

const validateEmail = () => {
  const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
  if (!emailRegex.test(email.value)) {
    emailError.value = "请输入有效的邮箱地址";
    return false;
  }
  emailError.value = "";
  return true;
};
</script>
```

## 四、目录结构最佳实践

### 4.1 功能模块结构 (`src/features/`)

核心概念：每个功能模块都是一个自包含的业务单元，包含该功能的所有相关代码。

功能模块模板结构：

```plaintext
src/features/
├── {feature-name}/ # 功能模块目录
│   ├── api/ # 功能API调用
│   │   ├── {feature}-api.ts # API 接口定义
│   │   └── types.ts # API 类型定义
│   ├── components/ # 功能专用组件
│   ├── composables/ # 功能专用组合式函数
│   ├── stores/ # 功能状态管理
│   │   └── {feature}Store.ts # Pinia Store
│   ├── types/ # 功能类型定义
│   ├── utils/ # 功能工具函数
│   ├── constants/ # 功能常量
│   ├── __tests__/ # 功能测试文件
│   └── index.ts # 模块入口（公共API）
```

### 4.2 全局共享组件 (`src/components/`)

遵循原子设计原则：

```plaintext
src/components/
├── ui/ # 基础UI组件（原子级）
│   ├── BaseButton/
│   ├── BaseInput/
│   └── ...
├── forms/ # 表单组件（分子级）
│   ├── FormInput/
│   └── ...
├── layout/ # 布局组件
│   ├── AppHeader/
│   └── ...
└── index.ts # 全局组件注册
```

### 4.3 全局组合式函数 (`src/composables/`)

- **全局共享的 Hook**：在多个功能或整个应用中复用
- **与具体业务无关的工具性逻辑**
- **提供通用的数据处理能力**

```plaintext
src/composables/
├── api/ # API 相关
│   ├── useApi.ts # 通用 API 调用
│   └── useFetch.ts # 数据获取逻辑
├── state/ # 状态管理
│   ├── useLocalStorage.ts # 本地存储
│   └── ...
└── index.ts # 统一导出
```

> **重要规则**：功能特定的 Hook 应放在 `features/[feature-name]/composables/`，而非全局 `composables/`。

### 4.4 应用服务层 (`src/services/`)

```plaintext
src/services/
├── api/ # API 服务
│   ├── httpClient.ts # HTTP 客户端
│   ├── interceptors.ts # 请求拦截器
│   └── errorHandler.ts # 错误处理
├── storage/ # 存储服务
│   ├── localStorage.ts
│   └── ...
└── index.ts
```

### 4.5 全局状态管理 (`src/stores/`)

```plaintext
src/stores/
├── appStore.ts # 应用全局状态
├── uiStore.ts # UI 状态管理
├── notificationStore.ts # 通知状态
└── index.ts
```

### 4.6 全局类型定义 (`src/types/`)

```plaintext
src/types/
├── global.d.ts # 全局类型声明
├── api.types.ts # API 响应类型
├── common.types.ts # 通用类型
└── index.ts
```

## 五、最佳实践

### 5.1 代码组织原则

#### 单一职责原则

```typescript
// ✅ 正确 - 每个文件只负责一个明确的功能
// useAuth.ts - 只处理认证逻辑
// authStore.ts - 只管理认证状态
// authApi.ts - 只处理认证 API 调用

// ❌ 避免 - 一个文件包含多个不相关的功能
// auth.ts - 包含认证逻辑、状态管理、API 调用
```

#### 依赖方向规则

```plaintext
// ✅ 正确 - 依赖方向从具体到抽象
components/ → composables/ → stores/ → api/ → types/

// ❌ 避免 - 循环依赖
components/ ←→ composables/
```

### 5.2 性能优化建议

#### 组件懒加载

```typescript
// 路由懒加载
const LoginPage = () => import("@/pages/LoginPage.vue");

// 组件懒加载
const HeavyComponent = defineAsyncComponent(
  () => import("@/components/HeavyComponent.vue")
);
```

#### 组合式函数优化

```typescript
// 使用 computed 和 watchEffect 优化响应式
const useOptimizedComposable = () => {
  const data = ref([]);
  const searchQuery = ref("");

  // ✅ 使用 computed 缓存计算结果
  const filteredData = computed(() =>
    data.value.filter((item) => item.name.includes(searchQuery.value))
  );

  // ✅ 使用 watchEffect 自动跟踪依赖
  watchEffect(() => {
    if (searchQuery.value) {
      // ...
    }
  });
};
```

### 5.3 测试策略

单元测试结构：

```plaintext
src/features/auth/__tests__/
├── composables/
│   ├── useAuth.spec.ts
│   └── useUserPermissions.spec.ts
├── stores/
│   └── authStore.spec.ts
└── components/
    └── LoginForm.spec.ts
```

测试示例：

```typescript
// features/auth/__tests__/useAuth.spec.ts
import { describe, it, expect, vi } from "vitest";
import { useAuth } from "../composables/useAuth";
import { useAuthStore } from "../stores/authStore";

// Mock 依赖
vi.mock("../stores/authStore");
vi.mock("../api/authApi");

describe("useAuth", () => {
  it("should login successfully", async () => {
    const { login, loading, error } = useAuth();
    expect(loading.value).toBe(false);
    const loginPromise = login({
      email: "test@example.com",
      password: "password",
    });
    expect(loading.value).toBe(true);
    await loginPromise;
    expect(loading.value).toBe(false);
    expect(error.value).toBeNull();
  });
});
```

## 六、开发流程与工具建议

### 6.1 开发流程

1. **创建新功能模块**

   ```bash
   mkdir -p src/features/new-feature/{api,components,composables,stores,types,utils,constants,__tests__}
   ```

2. **实现模块核心文件**

   ```typescript
   // features/new-feature/index.ts
   // 导出公共 API
   export { default as NewComponent } from "./components/NewComponent/NewComponent.vue";
   export { useNewFeature } from "./composables/useNewFeature";
   export { useNewFeatureStore } from "./stores/newFeatureStore";
   export type { NewFeatureType } from "./types";
   ```

3. **在应用中使用**
   ```typescript
   // 在页面或组件中
   import { useNewFeature, NewComponent } from "@/features/new-feature";
   ```

### 6.2 工具链建议

- **代码格式化**: Prettier
- **代码检查**: ESLint
- **类型检查**: TypeScript
- **测试**: Vitest + Vue Test Utils
- **文档生成**: VueDoc
- **性能分析**: Lighthouse

### 6.3 代码整洁度

- **及时删除** `console.log`、`debugger` 和注释掉的废弃代码
- 单文件建议不超过 **600 行**，超长需拆分组件或逻辑

## 七、添加原因

以下是对新增规范和建议的详细解释：

1. **类型定义规范（3.6）**

   - **原因**：清晰的类型定义能显著提升代码可读性和可维护性，减少类型错误。通过区分 `interface` 和 `type`，可以更好地组织类型定义，避免类型混乱。

2. **错误处理规范（3.7）**

   - **原因**：统一的错误处理机制可以确保应用在遇到错误时提供一致的用户体验，避免不同部分使用不同的错误处理方式导致的问题。

3. **国际化支持规范（3.8）**

   - **原因**：随着应用面向全球用户，国际化已成为必要功能。规范化的国际化配置和使用方式可以确保应用轻松支持多语言，避免重复代码和配置错误。

4. **性能优化建议（3.9）**

   - **原因**：良好的性能是用户体验的关键。通过组件优化、路由懒加载等手段，可以显著提升应用的加载速度和运行效率，特别是对于大型应用。

5. **安全规范（3.10）**

   - **原因**：Web 应用安全至关重要。规范的 XSS 防护和输入验证可以有效防止常见安全漏洞，保护用户数据和应用安全。

6. **功能模块结构优化（4.1）**

   - **原因**：清晰的功能模块结构使代码更易于组织和维护。每个功能模块自包含，降低了模块间的耦合度，提高了代码的可复用性。

7. **状态管理规范（Pinia）**

   - **原因**：Pinia 是 Vue 3 官方推荐的状态管理库，规范的使用方式可以确保状态管理清晰、可维护，避免状态管理混乱。

8. **API 类型定义（3.6.3）**

   - **原因**：API 类型定义与后端接口保持一致，可以减少前端和后端之间的沟通成本，提高开发效率，并在编译阶段捕获类型错误。

9. **依赖方向规则（5.1）**

   - **原因**：明确的依赖方向可以避免循环依赖问题，使应用结构更清晰，更容易进行代码重构和维护。

10. **代码整洁度（6.3）**
    - **原因**：保持代码整洁可以提高团队协作效率，减少 bug，使代码更易于理解和维护。

以上新增规范和建议基于 Vue 3 和 TypeScript 的最佳实践，并结合了大型项目的实战经验，旨在提升代码质量、开发效率和应用性能。
